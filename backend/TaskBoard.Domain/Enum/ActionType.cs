namespace TaskBoard.Domain.Enum
{
    public enum ActionType
    {
        CreateCard,
        UpdateCardName,
        UpdateCardPriority,
        UpdateCardDescription,
        Delete,
        AddCardToList
    }
}
