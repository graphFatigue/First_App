import { Observable } from "rxjs";
import { CardModel } from "src/app/_models/card/cardModel";

export const cardState: CardModel = {id: 0, name:'', actions:[], boardId:0, description:'', dueDate:'', listCardsName:'', priority:''};